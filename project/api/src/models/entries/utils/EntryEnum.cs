public enum EntryStatus {
    Draft,
    OnGoing,
    Done,
    Deleted,
    Stalled,
    Accomplished
}

public enum DeletedEntryStatus {
    Deleted,
    Cancelled,
    Ignored
}

public enum EntryType {
    Transaction,
    Commitment,
    Saving
}